mutation CreateUser(
    $userId: uuid!
    $userName: String!
    $email: String!
    $displayOrder: Int!
) {
    insertUser(
        objects: {
            userId: $userId
            userName: $userName
            email: $email
            displayOrder: $displayOrder
        }
    ) {
        returning {
            userId
            email
        }
    }
}
