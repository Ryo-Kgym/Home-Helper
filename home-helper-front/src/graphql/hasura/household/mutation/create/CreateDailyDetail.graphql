mutation CreateDailyDetail(
  $accountId: uuid!
  $amount: numeric!
  $genreId: uuid!
  $iocomeType: iocome_type!
  $categoryId: uuid!
  $date: date!
  $groupId: uuid!
  $id: uuid!
  $memo: String
  $userId: uuid!
) {
  insertDailyDetail(
    objects: {
      id: $id
      date: $date
      genreId: $genreId
      iocomeType: $iocomeType
      categoryId: $categoryId
      accountId: $accountId
      userId: $userId
      amount: $amount
      memo: $memo
      groupId: $groupId
    }
  ) {
    returning {
      id
    }
  }
}
