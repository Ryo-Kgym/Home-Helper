query GetAllGenre($groupId: uuid!) @cached {
    genre(where: {groupId: {_eq: $groupId}}, orderBy: {displayOrder: ASC}) {
        id: genreId
        genreName
        genreType
        iocomeType
        validFlag
        displayOrder
    }
}
