query GetAllUsers {
    users: user(orderBy: {displayOrder: ASC}) {
        id: userId
        name: userName
        email
        affiliation: affiliations {
            group {
                id: groupId
                name: groupName
            }
        }
    }
}