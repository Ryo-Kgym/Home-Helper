query GetAllUsers {
    users: allUsersList(orderBy: DISPLAY_ORDER_ASC) {
        id: userId
        name: userName
        group: groupByGroupId {
            id: groupId
            name: groupName
        }
    }
}
