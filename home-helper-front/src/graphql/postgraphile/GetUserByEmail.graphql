query GetUserByEmail($email: String!) {
    userByEmail(email: $email) {
        email
        userId
        userName
        groupByGroupId {
            groupId
            groupName
        }
    }
}
