query GetGenreTotalByMonth($fromDate: Date!, $toDate: Date!, $groupId: UUID!) {
    genreTotalByMonthList(fromDate: $fromDate, toDate: $toDate, groupId: $groupId) {
        date
        iocomeType
        genreId
        genreName
        total
    }
}
