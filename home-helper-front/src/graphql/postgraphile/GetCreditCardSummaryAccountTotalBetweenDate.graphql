query GetCreditCardSummaryAccountTotalBetweenDate(
    $fromDate: Date!
    $toDate: Date!
) {
    creditCardSummaryAccountTotalBetweenDateList(
        fromDate: $fromDate
        toDate: $toDate
    ) {
        accountId
        accountName
        total
        count
    }
}