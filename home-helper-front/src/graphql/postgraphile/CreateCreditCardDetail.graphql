mutation CreateCreditCardDetail(
    $id: UUID!
    $date: Date!
    $categoryId: UUID!
    $amount: BigFloat!
    $memo: String
    $summaryId: UUID!
) {
    createCreditCardDetail(
        input: {
            creditCardDetail: {
                id: $id
                date: $date
                categoryId: $categoryId
                amount: $amount
                summaryId: $summaryId
                memo: $memo
            }
        }
    ) {
        creditCardDetail {
            id
        }
    }
}
