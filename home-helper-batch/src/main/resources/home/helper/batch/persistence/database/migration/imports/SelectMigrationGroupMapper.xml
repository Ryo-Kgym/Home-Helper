<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="home.helper.batch.persistence.database.migration.imports.SelectMigrationGroupMapper">
    <select id="selectMigrationGroup"
            resultType="home.helper.batch.dto.migration.imports.ImportMigrationGroupOutput">
        select *
        from public.group
        order by
            group_id
    </select>
</mapper>