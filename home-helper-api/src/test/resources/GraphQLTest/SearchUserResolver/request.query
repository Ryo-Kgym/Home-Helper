query {
    searchUser(param: {}) {
        id
        name
        currentPoint
        lastHelp
    }
    findUserBy(userId: "1") {
        id
        name
        currentPoint
        lastHelp
    }
}

